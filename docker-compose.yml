version: '2'

services:
  web:
    env_file:
     - .env
    restart: always
    build: ./web
    expose:
      - "${PORT}"
    command: /usr/local/bin/gunicorn --log-file=- --log-level=debug --capture-output --config gunicorn.conf -w 2 -b :8000 live:application
    ports:
      - "${PORT}:8000"
